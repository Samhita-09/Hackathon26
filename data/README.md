The app auto-loads `healthcare-symptoms-disease.csv` from this folder.

Current file source:
- Mirrored symptom-disease training dataset compatible with the Kaggle symptom classification format.

Recommended run mode:
- Serve the folder with a local web server (for example `python -m http.server 8000`)
- Open `http://localhost:8000`
